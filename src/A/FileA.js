import React from "react";

function FileA() {
	// if (!Boolean(foo)) {
	// }
	const x = 2;
	Boolean(!x);

	els.forEach((el) => {
		f(el);
	});

	const a = ["a", "b", "c"];
	a.reduce((acc, val) => [...acc, val], []); // a.reduce((acc, val) => {acc.push(val); return acc}, []);

	const arr = [1, 2, 3];
	delete arr[0];

	const foo = new Set([1, 2, 3]);
	foo.delete(1);

	return (
		<div>
			<div>// comment</div>;<div dangerouslySetInnerHTML={createMarkup()}>FileA</div>
		</div>
	);
}

export const A = 0;

export default FileA;
